# generated by patch-package 6.4.14
#
# declared package:
#   @nestjs/schedule: 2.1.0
#
diff --git a/node_modules/@nestjs/schedule/dist/scheduler.registry.js b/node_modules/@nestjs/schedule/dist/scheduler.registry.js
index fea8811..e74034c 100644
--- a/node_modules/@nestjs/schedule/dist/scheduler.registry.js
+++ b/node_modules/@nestjs/schedule/dist/scheduler.registry.js
@@ -56,23 +56,26 @@ let SchedulerRegistry = class SchedulerRegistry {
     addCronJob(name, job) {
         const ref = this.cronJobs.get(name);
         if (ref) {
-            throw new Error((0, schedule_messages_1.DUPLICATE_SCHEDULER)('Cron Job', name));
+            //throw new Error((0, schedule_messages_1.DUPLICATE_SCHEDULER)('Cron Job', name));
+        } else {
+            this.cronJobs.set(name, job);
         }
-        this.cronJobs.set(name, job);
     }
     addInterval(name, intervalId) {
         const ref = this.intervals.get(name);
         if (ref) {
-            throw new Error((0, schedule_messages_1.DUPLICATE_SCHEDULER)('Interval', name));
+            //throw new Error((0, schedule_messages_1.DUPLICATE_SCHEDULER)('Interval', name));
+        } else {
+            this.intervals.set(name, intervalId);
         }
-        this.intervals.set(name, intervalId);
     }
     addTimeout(name, timeoutId) {
         const ref = this.timeouts.get(name);
         if (ref) {
-            throw new Error((0, schedule_messages_1.DUPLICATE_SCHEDULER)('Timeout', name));
+            //throw new Error((0, schedule_messages_1.DUPLICATE_SCHEDULER)('Timeout', name));
+        } else {
+            this.timeouts.set(name, timeoutId);
         }
-        this.timeouts.set(name, timeoutId);
     }
     getCronJobs() {
         return this.cronJobs;
